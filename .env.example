VITE_EMAILJS_SERVICE_ID=service_stsv
VITE_EMAILJS_TEMPLATE_ID=template_report
# Each teacher / parent enters their EmailJS public key inside the app (Profile page → EmailJS public key).
# That value is saved per-browser in localStorage. No public key needs to be set here.
